services:
  - type: web
    name: obrazovatelniy-hab
    env: node
    buildCommand: echo "Node.js server - no build needed"
    startCommand: node server.js
    healthCheckPath: /health.html
    plan: starter
    numInstances: 1
    sleepTimeout: never

version: "1"
environment:
  NODE_VERSION: "18"
